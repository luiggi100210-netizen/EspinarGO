import 'package:flutter/material.dart';

/// Paleta de colores oficial de EspinarGo.
/// Todos los colores de la app vienen de aquí.
class AppColors {
  // Marca - Naranja EspinarGo
  static const Color primary = Color(0xFFE8521A);
  static const Color primaryLight = Color(0xFFFEF0EA);
  static const Color primaryDark = Color(0xFFC44213);

  // Semánticos
  static const Color success = Color(0xFF16A34A);
  static const Color successLight = Color(0xFFDCFCE7);
  static const Color error = Color(0xFFDC2626);
  static const Color errorLight = Color(0xFFFEE2E2);
  static const Color warning = Color(0xFFD97706);
  static const Color warningLight = Color(0xFFFEF3C7);
  static const Color info = Color(0xFF2563EB);
  static const Color infoLight = Color(0xFFDEEAFF);

  // Conductor - Verde
  static const Color driver = Color(0xFF16A34A);
  static const Color driverLight = Color(0xFFDCFCE7);

  // Encomiendas - Azul
  static const Color package = Color(0xFF2563EB);
  static const Color packageLight = Color(0xFFDEEAFF);

  // Neutros
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textDisabled = Color(0xFF9CA3AF);
  static const Color background = Color(0xFFF9FAFB);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color border = Color(0xFFE5E7EB);
  static const Color borderLight = Color(0xFFF3F4F6);
  static const Color divider = Color(0xFFE5E7EB);

  // Mapa
  static const Color mapBackground = Color(0xFFE8E8E0);
  static const Color mapRoad = Color(0xFFFFFFFF);
  static const Color routeActive = Color(0xFF2563EB);

  // Overlay
  static const Color overlay = Color(0x80000000);
}